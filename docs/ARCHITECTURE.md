# 🏗️ Architecture Overview

Dokumentasi arsitektur teknis SIAKAD.

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        Client Layer                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Browser   │  │   Mobile    │  │   External Systems  │  │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘  │
└─────────┼────────────────┼────────────────────┼─────────────┘
          │                │                    │
          ▼                ▼                    ▼
┌─────────────────────────────────────────────────────────────┐
│                      Nginx / Load Balancer                  │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│                     Application Layer                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                    Laravel 12                        │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────────────┐  │   │
│  │  │  Routes   │ │Middleware │ │    Controllers    │  │   │
│  │  └─────┬─────┘ └─────┬─────┘ └─────────┬─────────┘  │   │
│  │        │             │                 │            │   │
│  │        ▼             ▼                 ▼            │   │
│  │  ┌─────────────────────────────────────────────┐   │   │
│  │  │              Service Layer                   │   │   │
│  │  │  KrsService | PenilaianService | etc.       │   │   │
│  │  └─────────────────────┬───────────────────────┘   │   │
│  │                        │                           │   │
│  │        ┌───────────────┼───────────────┐          │   │
│  │        ▼               ▼               ▼          │   │
│  │  ┌──────────┐  ┌──────────────┐  ┌──────────┐    │   │
│  │  │  Models  │  │ Repositories │  │   DTOs   │    │   │
│  │  └────┬─────┘  └──────┬───────┘  └──────────┘    │   │
│  └───────┼───────────────┼──────────────────────────┘   │
└──────────┼───────────────┼──────────────────────────────┘
           │               │
           ▼               ▼
┌─────────────────────────────────────────────────────────────┐
│                      Data Layer                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   MySQL     │  │    Redis    │  │      Storage        │  │
│  │  (Primary)  │  │   (Cache)   │  │   (Files/Logs)      │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 Directory Structure

```
siakad/
├── app/
│   ├── DTOs/                    # Data Transfer Objects
│   │   ├── KrsDTO.php
│   │   └── ...
│   ├── Helpers/                 # Global helper functions
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── Admin/           # Admin controllers (15+)
│   │   │   ├── Dosen/           # Dosen controllers (8)
│   │   │   ├── Mahasiswa/       # Mahasiswa controllers (10)
│   │   │   └── HealthController.php
│   │   ├── Middleware/
│   │   │   ├── RoleMiddleware.php
│   │   │   ├── SecurityHeadersMiddleware.php
│   │   │   └── RequestLoggingMiddleware.php
│   │   └── Requests/            # Form Request validation
│   ├── Models/                  # Eloquent models (22)
│   ├── Policies/                # Authorization policies
│   ├── Providers/               # Service providers
│   ├── Repositories/            # Data access layer
│   ├── Services/                # Business logic (10+)
│   │   ├── AkademikService.php
│   │   ├── KrsService.php
│   │   ├── PenilaianService.php
│   │   └── ...
│   └── Traits/                  # Reusable traits
├── config/
│   ├── siakad.php              # App-specific configuration
│   └── ...
├── database/
│   ├── factories/              # Model factories (6)
│   ├── migrations/             # DB migrations (27)
│   └── seeders/                # Seed data (12)
├── docs/                       # Documentation
├── resources/
│   └── views/
│       ├── admin/              # Admin views
│       ├── dosen/              # Dosen views
│       ├── mahasiswa/          # Mahasiswa views
│       ├── components/         # Blade components
│       └── layouts/            # Layout templates
├── routes/
│   ├── web.php                 # Main routes (226 lines)
│   └── auth.php                # Authentication routes
└── tests/
    ├── Feature/                # Feature tests (57)
    └── Unit/                   # Unit tests
```

---

## 🔄 Request Lifecycle

```
1. HTTP Request
       │
       ▼
2. Nginx (reverse proxy, SSL termination)
       │
       ▼
3. PHP-FPM
       │
       ▼
4. Laravel Bootstrap
       │
       ▼
5. Global Middleware
   ├── SecurityHeadersMiddleware (add security headers)
   └── VerifyCsrfToken (CSRF protection)
       │
       ▼
6. Route Matching
       │
       ▼
7. Route Middleware
   ├── auth (authentication check)
   ├── role:admin/dosen/mahasiswa (authorization)
   └── throttle:krs (rate limiting)
       │
       ▼
8. Controller
   └── Validates via Form Request
       │
       ▼
9. Service Layer
   └── Business logic execution
       │
       ▼
10. Repository / Model
    └── Database operations
       │
       ▼
11. Response (Blade view / JSON)
```

---

## 🗃️ Database Schema

### Core Entities

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│    users     │     │   fakultas   │     │    prodi     │
├──────────────┤     ├──────────────┤     ├──────────────┤
│ id           │     │ id           │     │ id           │
│ name         │     │ nama         │     │ nama         │
│ email        │     │ kode         │     │ fakultas_id  │──┐
│ password     │     └──────────────┘     └──────────────┘  │
│ role         │                                    ▲       │
└──────┬───────┘                                    │       │
       │                                            │       │
       ▼                                            │       │
┌──────────────┐     ┌──────────────┐              │       │
│  mahasiswa   │     │    dosen     │              │       │
├──────────────┤     ├──────────────┤              │       │
│ id           │     │ id           │              │       │
│ user_id      │──┐  │ user_id      │──┐           │       │
│ nim          │  │  │ nidn         │  │           │       │
│ prodi_id     │──┼──│ prodi_id     │──┼───────────┘       │
│ dosen_pa_id  │──┼──│              │  │                   │
└──────────────┘  │  └──────────────┘  │                   │
                  │                    │                   │
                  └────────────────────┴───────────────────┘
```

### Academic Flow

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│ tahun_akademik│    │     krs      │     │  krs_detail  │
├──────────────┤     ├──────────────┤     ├──────────────┤
│ id           │◄────│ tahun_akad_id│     │ krs_id       │
│ nama         │     │ mahasiswa_id │     │ kelas_id     │
│ semester     │     │ status       │     └──────┬───────┘
│ is_active    │     └──────────────┘            │
└──────────────┘                                 ▼
                                          ┌──────────────┐
┌──────────────┐     ┌──────────────┐     │    kelas     │
│  mata_kuliah │◄────│              │◄────├──────────────┤
├──────────────┤     │    nilai     │     │ matakuliah_id│
│ id           │     ├──────────────┤     │ dosen_id     │
│ kode         │     │ mahasiswa_id │     │ kapasitas    │
│ nama         │     │ kelas_id     │     └──────────────┘
│ sks          │     │ nilai_angka  │
└──────────────┘     │ nilai_huruf  │
                     └──────────────┘
```

---

## 🔐 Security Architecture

### Middleware Stack

```php
// Global (applies to all requests)
SecurityHeadersMiddleware::class

// Route Groups
'auth'     => Authenticate::class
'role'     => RoleMiddleware::class
'throttle' => ThrottleRequests::class
```

### Authorization Flow

```
Request → auth middleware → role middleware → Policy check → Controller
                │                │                  │
                ▼                ▼                  ▼
           401 if not      403 if wrong      403 if policy
           authenticated   role              denies
```

---

## 📊 Caching Strategy

| Data Type | Cache Store | TTL | Invalidation |
|-----------|-------------|-----|--------------|
| Config | File | Forever | `config:cache` |
| Routes | File | Forever | `route:cache` |
| Views | File | Forever | `view:cache` |
| Master Data | Redis/DB | 3600s | On update |
| Academic Year | Redis/DB | 3600s | On activation |

---

## 🧪 Testing Architecture

```
tests/
├── Feature/
│   ├── Auth/           # Authentication tests
│   ├── Admin/          # Admin CRUD tests
│   ├── Dosen/          # Dosen flow tests
│   ├── Mahasiswa/      # Mahasiswa flow tests
│   ├── Krs/            # KRS workflow tests
│   └── Penilaian/      # Grading tests
└── Unit/
    └── ExampleTest.php
```

### Test Categories

| Type | Purpose | Database |
|------|---------|----------|
| Feature | HTTP/Integration | RefreshDatabase |
| Unit | Isolated logic | None |

---

## 🔄 Deployment Pipeline

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  Push   │────▶│   CI    │────▶│  Build  │────▶│ Deploy  │
│  Code   │     │  Tests  │     │ Assets  │     │ Server  │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
                    │
                    ▼
              ┌─────────────┐
              │ PHPUnit/Pest│
              │ Pint (lint) │
              │ Coverage    │
              └─────────────┘
```
